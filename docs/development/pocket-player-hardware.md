# M6 初回実機チェック

Firmware 2.6 向けの最初のアルバムプレイヤー候補です。
設定は起動ごとに **通常順／2周＋5秒フェード／シャッフルOFF** に戻り、
自動再生はしません。実機での表示・音声・操作は未確認です。

## 導入

1. `out/build/m6-player-candidate-r1.zip` を展開し、`Cores`・`Assets`・
   `Platforms` をSDカードのルートへコピーします。
2. Pocket の openFPGA から **RPCMP Album Player** を選びます。
   ファイル選択が出たら `player.json` を選びます。
3. プレイヤー画面まで進むこと、勝手に音が鳴らないことを確認します。

この候補は `RPCMP.AlbumPlayer`／`rpcmp_player` 専用です。
従来のM5診断コアとは別に置けます。`Saves` は使用しません。

ZIP: **1,322,817 bytes**

SHA-256: `9341c4d21f1815f98ad2aeb590b3d812a5bbda38c7da159f40808fb5d127294d`

## 最初に確認する操作

同梱音源は自作のFMテスト音で、2アルバム・3曲です。
FMチャンネルAだけを使用し、左右両方へ出力する指定です。

1. 起動直後は **B** でアルバム一覧へ。十字キーで選び、**A** でアルバム、
   さらに曲を決定します。最初のアルバムの「上昇ループ」から試します。
2. 音が聞こえること、左右両方から出ること、曲名・時間・チャンネルAの
   演奏表示が動くことを確認します。
3. 再生操作へフォーカスを戻して **A** で一時停止／再開、**B** で停止、
   停止後にもう一度 **B** で一覧へ戻れることを確認します。
4. 再生しながら表示切替アイコンでTracker・鍵盤・曲一覧を切り替え、
   音が途切れないか確認します。**L/R** で前後の曲へ切り替えます。
5. ループアイコンを十字キーで選び **A** を押すと、2周→3周→5周→無限を
   切り替えます。シャッフルもアイコンで切り替えます。

通常設定で「上昇ループ」を最後まで流すと、2周＋5秒フェードの後に
「下降・終端」へ進み、その曲が自然に終わると停止する想定です。
一時停止・表示切替・無限ループの確認は、この自動送り確認と分けて行えます。

通常再起動と電源OFF後も起動でき、変更した設定が既定値へ戻ることを
確認してください。まずは下の結果だけで十分です。100〜300曲の実曲検証や
詳しいサービス時間測定は、初回の表示・音声が確認できてから進めます。

```text
Firmware: 2.6
起動 / 勝手に再生しない:
アルバム一覧 / 曲決定 / 日本語表示:
音楽 / 左右 / 無限ループで1分:
A一時停止・再開 / B停止・一覧 / L・R:
Tracker / 鍵盤 / 曲一覧へ切替中の音:
2周＋フェード / 次曲 / アルバム末尾停止:
ループ・シャッフルのアイコン操作:
通常再起動 / 電源OFF後 / 設定リセット:
気になった表示・操作・エラー:
```

途中で止まった場合は、最後の画面の文字と操作をそのまま記録してください。
エラー画面のサービス間隔表示も判断材料になります。テストを通過しても、
実曲負荷での音声供給期限や既存シェルの外部タイミングまで確認済みにはなりません。
