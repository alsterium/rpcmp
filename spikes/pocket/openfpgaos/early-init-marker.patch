diff --git a/src/firmware/os/kernel/main.c b/src/firmware/os/kernel/main.c
--- a/src/firmware/os/kernel/main.c
+++ b/src/firmware/os/kernel/main.c
@@ -25 +25 @@
-#ifdef RPCMP_EARLY_MARKER
+#if defined(RPCMP_EARLY_MARKER)
@@ -392 +392 @@ void os_main(void) {
-#ifdef RPCMP_EARLY_MARKER
+#if defined(RPCMP_EARLY_MARKER) && RPCMP_EARLY_MARKER == 1
@@ -412,1 +412,5 @@ void os_main(void) {
+#if defined(RPCMP_EARLY_MARKER) && RPCMP_EARLY_MARKER == 2
+    rpcmp_early_marker();
+#endif
+
     /* Boot stage: red logo = OS initializing */
